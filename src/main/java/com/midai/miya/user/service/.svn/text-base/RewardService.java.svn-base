package com.midai.miya.user.service;

import com.midai.miya.user.model.Reward;
import com.midai.miya.utils.PageUtil;

import java.util.List;

public interface RewardService {

     List<Reward> findByConditions(Reward reward,PageUtil page);

     long findByConditionsCount(Reward reward);

     void save(Reward reward);

     void update(Reward reward);

     void delete(Reward reward);

}