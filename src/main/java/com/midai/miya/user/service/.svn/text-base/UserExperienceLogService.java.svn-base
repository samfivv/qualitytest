package com.midai.miya.user.service;

import com.midai.miya.user.model.UserExperienceLog;
import java.util.List;
import com.midai.miya.utils.PageUtil;

public interface UserExperienceLogService {

     List<UserExperienceLog> findByConditions(UserExperienceLog userExperienceLog,PageUtil page);

     long findByConditionsCount(UserExperienceLog userExperienceLog);

     void save(UserExperienceLog userExperienceLog);

     void update(UserExperienceLog userExperienceLog);

     void delete(UserExperienceLog userExperienceLog);

}