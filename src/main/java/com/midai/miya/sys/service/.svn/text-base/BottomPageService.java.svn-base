package com.midai.miya.sys.service;

import com.midai.miya.sys.model.BottomPage;
import java.util.List;
import com.midai.miya.utils.PageUtil;

public interface BottomPageService {

     List<BottomPage> findByConditions(BottomPage bottomPage,PageUtil page);

     long findByConditionsCount(BottomPage bottomPage);

     void save(BottomPage bottomPage);

     void update(BottomPage bottomPage);

     void delete(BottomPage bottomPage);


   	BottomPage findBottomPageById(String bottomPageId);

   	int findBottomPageCountByName(BottomPage bottomPage);
}